# =============================================================================
# AMSF001 Project Tracker - Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to version control
# =============================================================================

# -----------------------------------------------------------------------------
# SUPABASE CONFIGURATION (Required)
# -----------------------------------------------------------------------------
# Get these from your Supabase project settings > API
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# -----------------------------------------------------------------------------
# ANTHROPIC API (Required for AI Chat feature)
# -----------------------------------------------------------------------------
# Get this from https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api03-your-key

# -----------------------------------------------------------------------------
# OPTIONAL CONFIGURATION
# -----------------------------------------------------------------------------

# Project Reference (for multi-tenant support, defaults to AMSF001)
VITE_PROJECT_REF=AMSF001

# Enable debug mode (shows extra logging in console)
VITE_DEBUG=false

# =============================================================================
# NOTES
# =============================================================================
# 
# 1. VITE_ prefix is required for client-side variables in Vite
#    Variables without VITE_ prefix are only available server-side
#
# 2. ANTHROPIC_API_KEY is used server-side only (in api/chat.js)
#    It should NEVER have the VITE_ prefix
#
# 3. For Vercel deployment, set these in Project Settings > Environment Variables
#
# 4. The Supabase anon key is safe to expose client-side (it's designed for this)
#    Row Level Security (RLS) protects your data
#
# =============================================================================
